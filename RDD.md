# Smart-INVOICE 要件定義書

| 項目 | 内容 |
|------|------|
| システム名称 | Smart INVOICE（製品表示名に準ずる） |
| 文書種別 | 要件定義書 |
| 対象バージョン | アプリケーション `package.json` の `version`（現行: 1.1.0）に準拠 |
| 関連文書 | `DESIGN.md`（設計書）、`README.md`（環境構築・運用概要） |

---

## 1. はじめに

### 1.1 目的

本書は、請求書生成業務における **利用実績データの取り扱いをアプリケーションで標準化し、人的ミスを抑える** ことを目的とするシステム（smart-invoice）について、**利害関係者が合意すべき要件** を定義する。

詳細な実装・構成は **設計書** に委ね、本書は **何を満たすか（What）** を中心に記載する。

### 1.2 背景

BI ツール（Apache Superset 等）から出力される **利用実績 CSV** を基に請求処理を行ってきたが、手作業・転記・集計の段階でミスが混入しうる。当該業務を **画面操作とデータベース永続化** により再現可能な手順に置き換える必要がある。

### 1.3 スコープ（対象）

以下を本システムの対象とする。

- Web ブラウザ（**Google Chrome 上での適切な動作** を満たすこと）および、同一コードベースに基づく **デスクトップ（Electron）** からの利用
- **顧客（請求先）マスタ** の参照・登録・更新・削除
- **お客様利用データ（CSV）** の取り込み、サーバ側での **日次利用実績の保存** と **請求発行メタ情報の管理**
- 保存済みデータに基づく **請求書 Excel の生成とダウンロード**
- バックエンド API の **ヘルスチェック**（稼働・連携確認用）

### 1.4 スコープ外（明示的な非対象）

以下は **現行要件では対象外** とする（必要になった場合は別途要件追加とする）。

- Superset との **自動 API 連携**（ユーザーは CSV を手動で取得し、本システムへ読み込む）
- **ユーザー認証・認可・監査ログ** をアプリケーション層で完結する仕様（別途ネットワーク・運用で制御する前提を許容する）
- **会計システムへの自動連携**、**電子請求書（e-インボイス等）の送信**
- **請求書 PDF の公式フォーマット固定**（Excel 出力が主成果物）

### 1.5 用語

| 用語 | 説明 |
|------|------|
| お客様利用データ | 店舗・日次のラベル数・商品更新数等を含む CSV。必須列は設計書・実装の検証ロジックに従う |
| 利用年月 | 請求の対象月。`YYYY-MM` 形式で扱う |
| 請求書番号 | 取込完了時にサーバが採番する番号。同一利用年月内での連番の扱いは設計書に従う |
| 店舗集計 | 請求書 Excel 生成のため、店舗別の利用日数・平均ラベル数等に加工したデータ |

---

## 2. 利害関係者・利用者

| 役割 | 主な関心 |
|------|-----------|
| 請求・経理担当 | 正確な請求書の作成、作業時間の短縮、誤入力の防止 |
| システム運用担当 | デプロイ、DB バックアップ、API とフロントの疎通 |
| 開発・保守担当 | 要件の明確化、設計書とのトレーサビリティ |

---

## 3. 前提・制約

### 3.1 技術・環境

- **実装言語**: TypeScript を主とする
- **フロント**: React
- **ブラウザ**: Google Chrome での動作互換を満たす
- **ランタイム**: Node.js / npm のバージョンはリポジトリの `engines` で **チーム間で固定** する
- **データベース**: PostgreSQL（開発環境ではコンテナ等を用いる想定に準拠）

### 3.2 データ連携形式

- サーバへの **生の multipart ファイルアップロード** を必須としない。**クライアントで CSV を解析したうえで JSON として API に送信** する方式を満たす。
- API リクエストボディの上限は、運用上妥当な範囲でサーバ設定により制限される（設計書の値に従う）。

### 3.3 配布形態

- **PWA** としての利用に対応できること
- **Electron** によるデスクトップ配布が可能であること。デスクトップ利用時も **バックエンド API が別途利用可能** であることを前提とする

---

## 4. 機能要件

### 4.1 ホーム・ナビゲーション

| ID | 要件 |
|----|------|
| FR-HOME-01 | 利用者が主要機能（顧客管理、利用データ取込、請求書発行等）へ遷移できること |

### 4.2 顧客マスタ管理

| ID | 要件 |
|----|------|
| FR-CUST-01 | 顧客一覧を参照できること（一覧の並び順は業務上わかりやすい基準を満たすこと。現行実装では会社名昇順） |
| FR-CUST-02 | 顧客を新規登録できること。科目として **会社コード・会社名・SI パートナー名・単価・通貨** を保持すること |
| FR-CUST-03 | **会社コードの一意性** を満たすこと。重複時は利用者が判別できるエラーとすること |
| FR-CUST-04 | 単価は **正の数値** であること。不正値は登録・更新を拒否すること |
| FR-CUST-05 | 登録済み顧客を更新・削除できること |

### 4.3 お客様利用データの取込

| ID | 要件 |
|----|------|
| FR-UP-01 | 利用者が **対象顧客** と **利用年月（YYYY-MM）** を指定し、CSV を選択できること |
| FR-UP-02 | CSV に **必須列**（`Day`, `Company`, `Store`, `Total Labels`, `Product Updated`）が含まれること。欠落時は取込せず、理由が分かる形で利用者に示すこと |
| FR-UP-03 | CSV 内の顧客（Company）は **選択顧客と一致する単一の値** であること。複数顧客が混在する場合は取込しないこと |
| FR-UP-04 | 各レコードの日付が **指定した利用年月と整合** すること。不整合は取込しないこと |
| FR-UP-05 | 数値項目が妥当であること（非数等は拒否） |
| FR-UP-06 | 取込成功時、サーバは **当該顧客・当該月の日次利用実績を保存** し、既存の同月データがある場合は **当該月分を取込内容で置き換える** 挙動を満たすこと（詳細は設計書） |
| FR-UP-07 | 同一顧客・同一利用年月に対し **発行メタ情報を更新または新規発行** し、**請求書番号の採番規則** を満たすこと（詳細は設計書） |
| FR-UP-08 | オプション列 `Name` が欠ける場合、**店舗マスタがあれば店舗名を補完** できること（マスタ未登録時の挙動は実装に従い、利用者が後続で確認可能であることを優先する） |

### 4.4 請求書 Excel の発行

| ID | 要件 |
|----|------|
| FR-EX-01 | 利用者が顧客を選び、**当該顧客の発行済み請求一覧** を参照できること |
| FR-EX-02 | 利用者が **請求日・支払期限** 等、画面で必要な日付・項目を指定し、Excel を生成できること |
| FR-EX-03 | Excel は **所定のテンプレート** に基づき、顧客情報・利用年月・店舗集計・金額・通貨等を反映すること |
| FR-EX-04 | 店舗別集計には、**利用開始日以降の実績に基づく指標**（利用日数、平均ラベル数、平均更新数等）を含めること（算出定義は設計書・SQL に従う） |
| FR-EX-05 | 利用者が **超過課金（ペナルティ）** を選択した場合、**商品更新率が所定閾値を超える店舗** に対し、別途定義された計算を Excel に反映すること（閾値・式は実装に準拠） |

### 4.5 バックエンド API

| ID | 要件 |
|----|------|
| FR-API-01 | ヘルスチェック用エンドポイントを提供し、フロントまたは運用から疎通確認できること |
| FR-API-02 | 顧客の CRUD を REST 風 API で提供すること |
| FR-API-03 | 利用データの取込、発行済み一覧取得、店舗集計取得を API で提供すること |
| FR-API-04 | エラー時、利用者または運用者が原因を一定程度特定できる **メッセージまたはコード** を返すこと |

---

## 5. 非機能要件

### 5.1 可用性・運用

| ID | 要件 |
|----|------|
| NFR-OPS-01 | 本番運用における **フロントの配信** と **API の配置** の手順が文書化されていること（`README.md` 等） |
| NFR-OPS-02 | DB スキーマ変更は **マイグレーション** により再現可能な手順で実施できること |

### 5.2 保守性

| ID | 要件 |
|----|------|
| NFR-MAINT-01 | 未使用コードを過剰に蓄積しない方針に従い、変更影響が把握しやすい構成であること（ワークスペース開発ルールに準拠） |
| NFR-MAINT-02 | 可読性のため、必要に応じてコメントを付与すること |
| NFR-MAINT-03 | 共通化が妥当な UI・ロジックは再利用しうる形で整理すること |

### 5.3 性能・拡張（目安）

| ID | 要件 |
|----|------|
| NFR-PERF-01 | 通常想定される月次 CSV 件数において、取込・一覧表示・Excel 生成が **実務上許容される時間** 内で完了すること（具体数値は運用負荷に応じ別紙でも可） |

### 5.4 セキュリティ（現行の位置づけ）

| ID | 要件 |
|----|------|
| NFR-SEC-01 | 認証・認可をアプリに実装しない場合、**アクセス制御を別手段（NW・IIS・VPN 等）で補う** 旨を運用で管理すること |
| NFR-SEC-02 | 社内証明書等を扱うクライアント設定について、**リスクを理解した上で** 利用すること（Electron の証明書取扱い等） |

---

## 6. データ要件（業務視点）

### 6.1 保持すべき情報

- **顧客**: 請求に必要な識別子（会社コード）、名称、請求単価、通貨、請求元表記（SI パートナー名）
- **店舗マスタ**（任意データ）: 会社コード・店舗コードに紐づく店舗名
- **日次利用実績**: 会社・店舗・日付・ラベル合計・商品更新数
- **発行済み請求メタ**: 会社、利用年月、請求書番号、通貨、TTM 等、Excel 生成に必要な付帯情報

### 6.2 整合性

- 同一顧客・同一店舗・同一日付の利用実績は **二重に矛盾しない** よう、システムが一意性を満たすこと（DB 制約・設計に従う）

---

## 7. 外部インターフェース

| 相手 | 内容 |
|--------|------|
| 利用者 | CSV ファイル（UTF-8 想定）、ブラウザまたは Electron クライアント |
| PostgreSQL | 永続化 |
| デプロイ基盤 | README に記載の IIS・GitHub Actions 等（運用上のインターフェース） |

---

## 8. 受入れ・トレーサビリティ

- 機能要件 ID（FR-*）および非機能要件 ID（NFR-*）は、テスト観点・受入れチェックリストの整理に利用できる。
- 実装・API 詳細・画面遷移の対応は **`DESIGN.md`** およびソースコードで追う。

---

## 9. 改訂

| 版 | 日付 | 概要 |
|----|------|------|
| 1.0 | 2025-03-26 | 初版（設計書・ソースに基づく要件の逆引き整理） |

---

*本書は Smart INVOICE の現行ソースおよび `DESIGN.md` に整合するよう記述している。ビジネス上の最終決裁はプロジェクトの責任者による承認を前提とする。*
